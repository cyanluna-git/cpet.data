# ============================================
# CPET Platform Frontend - 환경 변수 예제
# ============================================
#
# 주의: 프론트엔드는 루트 디렉토리의 .env 파일을 사용합니다.
# vite.config.ts가 자동으로 상위 폴더의 .env를 로드합니다.
#
# 모든 환경 변수는 /cpet.db/.env 파일에서 관리하세요.
# ============================================

# 필요한 환경 변수:
# - BACKEND_PORT: 백엔드 서버 포트 (기본값: 8100)
# - FRONTEND_PORT: 프론트엔드 서버 포트 (기본값: 3100)
# - VITE_API_URL: (선택) API URL 직접 지정 (기본값: http://localhost:${BACKEND_PORT})
