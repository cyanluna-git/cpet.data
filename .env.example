# ============================================
# CPET Platform - 환경 변수 설정
# 이 파일을 .env로 복사하여 사용하세요
# ============================================

# --------------------------------------------
# Database (SQLite - 기본값)
# --------------------------------------------
# SQLite 파일 경로 (기본값: cpet.db)
SQLITE_DB_PATH=cpet.db

# DATABASE_URL을 직접 지정하지 않으면 SQLite가 자동 사용됩니다
# DATABASE_URL=sqlite+aiosqlite:///./cpet.db

# --------------------------------------------
# Database (PostgreSQL - 선택사항)
# 필요시 아래 주석 해제 후 사용
# --------------------------------------------
# DB_HOST=localhost
# DB_PORT=5100
# DB_USER=cpet_user
# DB_PASSWORD=cpet_password
# DB_NAME=cpet_db
# DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# --------------------------------------------
# Backend (FastAPI)
# --------------------------------------------
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8100

# Security (반드시 변경하세요!)
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
ENCRYPTION_KEY=your-encryption-key-here

# Application
APP_NAME=CPET Platform
DEBUG=true

# File Upload
MAX_UPLOAD_SIZE=10485760
UPLOAD_DIR=./uploads

# Analysis Settings
DEFAULT_SMOOTHING_WINDOW=10
DEFAULT_CALC_METHOD=Frayn

# --------------------------------------------
# Frontend (React + Vite)
# --------------------------------------------
FRONTEND_PORT=3100
VITE_API_URL=http://localhost:8100

# --------------------------------------------
# CORS Settings
# --------------------------------------------
ALLOWED_ORIGINS=http://localhost:3100
