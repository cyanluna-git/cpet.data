# CPET.db AI Skills Setup Guide

당신의 CPET.db 프로젝트를 위해 Vercel의 agent-skills 프레임워크를 기반으로 커스텀화한 AI 스킬 시스템입니다.

## 📁 디렉토리 구조

```
.ai-skills/
├── react-best-practices/
│   ├── rules/                          # 개별 규칙 파일
│   │   ├── _sections.md               # 섹션 메타데이터
│   │   ├── _template.md               # 새 규칙 생성 템플릿
│   │   ├── async-*.md                 # 워터폴 제거 규칙
│   │   ├── bundle-*.md                # 번들 최적화 규칙
│   │   ├── client-*.md                # 클라이언트 데이터 페칭
│   │   ├── server-*.md                # 서버 성능 규칙
│   │   └── ...
│   ├── src/                           # 빌드 스크립트
│   ├── metadata.json                  # 문서 메타데이터
│   ├── SKILL.md                       # 스킬 개요
│   ├── README.md                      # 사용 가이드
│   └── AGENTS.md                      # 컴파일된 규칙 (자동 생성)
│
└── deployment-guidelines/
    ├── rules/                         # 배포 가이드
    │   ├── vercel-*.md               # Vercel 배포 가이드
    │   ├── cloud-run-*.md            # Cloud Run 배포 가이드
    │   └── supabase-*.md             # Supabase 설정
    └── README.md
```

## 🚀 빠른 시작

### 1단계: 의존성 설치

```bash
cd /Users/cyanluna-pro16/dev/cpet.db/.ai-skills/react-best-practices
pnpm install
```

### 2단계: 규칙 검증 및 빌드

```bash
pnpm validate    # 규칙 파일 유효성 확인
pnpm build       # AGENTS.md 생성
```

### 3단계: 스킬 통합

당신의 IDE나 LLM 도구에서 이 스킬을 참고하세요:

- **SKILL.md** - 스킬 개요 및 빠른 참조
- **AGENTS.md** - 모든 규칙의 완전한 가이드
- **rules/** - 개별 규칙 파일 (상세 설명)

## 📚 규칙 카테고리

| 우선순위 | 카테고리 | 영향 | 파일 접두사 |
|---------|---------|------|----------|
| 1 | 워터폴 제거 | CRITICAL | `async-` |
| 2 | 번들 최적화 | CRITICAL | `bundle-` |
| 3 | 서버 성능 | HIGH | `server-` |
| 4 | 클라이언트 데이터 페칭 | MEDIUM-HIGH | `client-` |
| 5 | 리렌더 최적화 | MEDIUM | `rerender-` |
| 6 | 렌더링 성능 | MEDIUM | `rendering-` |
| 7 | JavaScript 성능 | LOW-MEDIUM | `js-` |
| 8 | 고급 패턴 | LOW | `advanced-` |
| 9 | CPET 특화 패턴 | MEDIUM | `cpet-` |

## ✨ 주요 커스텀 규칙

당신의 CPET.db 스택에 맞는 규칙들이 포함되어 있습니다:

### 데이터베이스 (Supabase)
- **client-supabase-parallel.md** - Supabase 쿼리 병렬화
- **rerender-supabase-subscriptions.md** - 실시간 구독 캐싱
- **server-rls-security.md** - Row-Level Security 최적화

### API 통합 (Cloud Run)
- **server-batch-requests.md** - Cloud Run API 요청 배치 처리
- **server-server-components.md** - Next.js Server Components 활용

### 성능 최적화
- **rerender-cpet-memoization.md** - CPET 데이터 변환 메모이제이션
- **bundle-lazy-charts.md** - 대사 차트 레이지 로드
- **rendering-virtual-scroll.md** - CPET 데이터 테이블 가상 스크롤

## 🔧 새로운 규칙 작성

1. **템플릿 복사:**
```bash
cp rules/_template.md rules/[area]-[description].md
```

2. **규칙 파일 채우기:**
   - `---` 프론트매터에 메타데이터 작성
   - 규칙 제목 설정
   - 잘못된 예제와 올바른 예제 작성
   - 참고 자료 추가

3. **검증 및 빌드:**
```bash
pnpm validate
pnpm build
```

## 🏗️ 3계층 아키텍처

```
┌─────────────────────────────────┐
│  Vercel (Frontend)              │
│  - Next.js React Components     │
│  - Server Components            │
│  - Static Generation (ISR)      │
└──────────────┬──────────────────┘
               │ HTTP API
┌──────────────▼──────────────────┐
│  Google Cloud Run (Backend)     │
│  - FastAPI Application          │
│  - CPET Data Processing         │
│  - Authentication & Business    │
└──────────────┬──────────────────┘
               │ SQL
┌──────────────▼──────────────────┐
│  Supabase (Database)            │
│  - PostgreSQL Database          │
│  - Real-time Subscriptions      │
│  - Row-Level Security           │
└─────────────────────────────────┘
```

## 📖 문서 참고

- **[React Best Practices README](./react-best-practices/README.md)** - 개별 규칙 관리
- **[배포 가이드](./deployment-guidelines/README.md)** - 배포 환경별 설정
- **[Vercel 문서](https://vercel.com/docs)** - Next.js 배포
- **[Google Cloud Run](https://cloud.google.com/run/docs)** - FastAPI 배포
- **[Supabase 문서](https://supabase.com/docs)** - 데이터베이스 관리

## 🔄 정기적인 유지보수

```bash
# 월 1회: 규칙 업데이트 및 빌드
cd .ai-skills/react-best-practices
pnpm dev

# 규칙 파일 추가/수정 후
pnpm validate
pnpm build
```

## 💡 사용 시나리오

### 새로운 컴포넌트 작성
1. SKILL.md에서 해당 카테고리의 규칙 검토
2. "Correct" 예제를 기반으로 구현
3. 번들 크기와 성능 체크

### 성능 최적화
1. AGENTS.md에서 관련 규칙 찾기
2. "Incorrect" vs "Correct" 비교
3. 점진적으로 개선

### 새로운 기능 추가
1. 해당하는 규칙 확인 (async-*, client-*, server-*)
2. 배포 가이드에서 환경 설정 확인
3. CPET 특화 패턴 검토

## 📞 지원

규칙이나 배포 설정에 대한 질문이 있으시면:
1. 해당 규칙 파일의 "Reference" 섹션 확인
2. 배포 가이드의 관련 섹션 참고
3. Vercel/Supabase/GCP 공식 문서 참조

---

**버전:** 1.0.0  
**마지막 업데이트:** 2026년 1월 16일  
**기반:** Vercel agent-skills framework
